/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['akcdn.detik.net.id','awsimages.detik.net.id','asset-2.tstatic.net','img.okezone.com']
    }
}

module.exports = nextConfig
